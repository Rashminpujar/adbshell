

#To run this script, Setup adb service using below commands.

 sudo apt-get install  android-tools-adb 
 sudo apt install aapt 

#Displaying some device details like Android version, Ram, Display, Resolution and Manufacturer.

#Install the attached app inside the device before launching it.

#Get the install app package name.

#Closing the most recently used app from the device screen.

#Launching the app using that package name.

#Enter the Username and pin manually to login.

#Enter the info using adb input events to search the flights

